# Module Ksqlite Internal Test

Internal module, not intended for public use.

Test-only utilities shared by `ksqlite-capi` and `ksqlite-kapi`'s own test suites, so neither has
to reimplement the same per-target plumbing on its own.